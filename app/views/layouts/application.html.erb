<!DOCTYPE html>
<html>
  <head>
   <title><%= content_for?(:page_title) ? yield(:page_title) : "SmokeFree" %></title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= yield :head %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <div data-controller="flash"
         data-flash-style-value="toast"
         data-flash-notice-value="<%= flash[:notice].to_s %>"
         data-flash-alert-value="<%= flash[:alert].to_s %>"></div>
    <%= yield %>
  </body>
</html>
